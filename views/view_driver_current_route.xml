<odoo>
    <data>
    <act_window 
    id="action_driver_current_route" 
    name="Ruta actual" 
    res_model="cxc.account" 
    view_id="view_driver_current_route" 
    view_mode="tree,form" 
    target="current" 
    domain="[]" 
    limit="80"/>

    <record id="view_driver_current_route" model="ir.ui.view">
        <field name="name">Ruta actual</field>
        <field name="model">cxc.account</field>
        <field name="arch" type="xml">
          <tree>
            <field name="customerId"/>
            <field name="name"/>
            <field name="charge"/>
            <button string="Agregar abono" class="oe_highlight" type="object" name="create_payment"/>
          </tree>
        </field>
    </record>
    </data>
</odoo>